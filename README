# Paso de instalacion de los modules
 
- python -m venv modules
- cd modules
- cd Scripts -- modo escucha
- activate

- raiz ==> pip freeze
- pip install -r package.txt

- pip install pymysql
- pip install 

